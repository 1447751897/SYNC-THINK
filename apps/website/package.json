{
  "name": "@sync-think/website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests && vitest run src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node --check auth.js && node --check site.js && node --check auth-client.js && node --check build.mjs && node --check scripts/build-demo.mjs"
  },
  "dependencies": {
    "@sync-think/protocol": "workspace:*",
    "@sync-think/shared": "workspace:*",
    "@sync-think/ui-kit": "workspace:*",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "@types/node": "^20.17.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4",
    "vitest": "^2.1.8"
  }
}
