# AI Development Rules

本文件是项目开发时给 AI 助手读取并严格执行的统一规范。每次开始新任务前，AI 必须先阅读本文件，并在执行过程中持续遵守。

## 0. 使用方式

在每个新项目开始时，把本文件放到项目根目录，推荐命名为：

- `AI_DEVELOPMENT_RULES.md`
- `CLAUDE.md`
- `AGENTS.md`
- `.cursor/rules/project.md`

首次对话时使用以下提示：

```text
请先阅读项目根目录下的 AI_DEVELOPMENT_RULES.md，并在本次开发全过程中严格执行。除非我明确要求变更规则，否则不要跳过、弱化或自行改写这些约束。
```

## 1. 核心原则

1. 先理解，再修改。
2. 优先遵循现有项目结构、技术栈、命名风格和代码习惯。
3. 不做与当前任务无关的重构。
4. 不臆造不存在的接口、文件、字段、依赖或业务规则。
5. 遇到不确定的信息，先从代码、配置、文档、测试中查证。
6. 修改必须小步、可验证、可回退。
7. 完成后必须说明改了什么、为什么改、如何验证。
8. **先完整阅读** `docs/superpowers/specs/2026-07-11-sync-think-product-design.md` 中与任务相关的章节；**Locked 决策不得重开**。
9. **第一原则：上下文连续性与用户控制模型调度。**

## 2. 开发前必须执行

开始写代码前，AI 必须完成以下检查：

1. 阅读项目结构，识别主要技术栈、入口文件、构建方式和测试方式。
2. 查找与当前需求相关的文件、组件、接口、状态管理、数据库模型或配置。
3. 如果已有相似功能，优先复用现有模式。
4. 如果需求不清楚，先提出最少数量的关键问题。
5. 如果可以根据上下文合理判断，则直接执行，不要频繁打断用户。
6. 涉及产品边界时对照 `docs/product/01-requirements-clarification.md` 与路线图。
7. 涉及 UI 时先读 `docs/product/15-frontend-design.md` 与 tokens。

禁止：

- 不看代码直接给方案。
- 用全新架构覆盖已有架构。
- 为了实现小功能引入大型依赖。
- 修改无关文件。
- 删除用户已有改动。
- 重新讨论已批准的 Locked 产品决策。
- 实现闭测非目标（Later 列表）却声称是 MVP 必须。

## 3. 修改代码规则

AI 修改代码时必须遵守：

1. 保持改动范围最小。
2. 保持文件职责清晰，不把无关逻辑塞进同一个文件。
3. 优先使用项目已有工具函数、组件、样式变量、请求封装和错误处理方式。
4. 新增代码必须可读、可维护，避免炫技。
5. 不写无意义注释，只在复杂逻辑前添加简短说明。
6. 不硬编码敏感信息、密钥、账号、真实 token、生产地址。
7. 不吞掉错误，必须保留合理的错误处理和用户反馈。
8. 不为了让测试通过而降低业务正确性。
9. 外部副作用前先持久化事件/意图；恢复路径必须可测。
10. 密钥永不进入 Renderer 状态、提示词、日志、诊断导出。

## 4. 前端开发规则

如果任务涉及前端，必须遵守：

1. 先读取 `docs/product/15-frontend-design.md` 和 `docs/product/15-frontend-design-tokens.json`；如果缺失或内容为空，必须先根据项目类型补齐 UI 风格参考、设计关键词和 token 值。
2. 所有颜色、间距、圆角、字体、阴影必须引用 `15-frontend-design-tokens.json` 中的 token 值，禁止硬编码随意值。如果需要新增 token，先添加到 token 文件再引用。
3. 优先匹配现有设计系统、组件库、间距、字体、颜色和交互习惯。
4. 页面必须在目标 Windows 分辨率与缩放下不出现明显布局错乱。
5. 文本不能溢出按钮、卡片、表格单元格或弹窗。
6. 表单必须有明确的校验、错误提示和提交状态。
7. 异步数据必须处理 loading、empty、error、success 状态。
8. 交互控件要符合用户直觉：开关用 switch/checkbox，选项用 select/tabs/segmented control，危险操作要二次确认。
9. 不做空洞的营销页，除非需求明确要求。
10. 不添加与业务无关的装饰性元素（无渐变光球、bokeh、环境装饰动画）。
11. 产品形态、UI 风格、布局、组件策略或交互状态发生变化时，必须反向更新 `docs/product/15-frontend-design.md`。
12. 遵守 Locked IA：左文件夹任务树、中完整聊天、右可折叠轨迹；完整浅/深主题；拒绝分裂主题。
13. **禁止 V3 换皮上线。** V3 只锁结构；视觉、组件、动效、记忆点必须按 `15-frontend-design.md` §0 做到可获奖级原创。
14. 动效须可被用户明确感知且服务状态理解；禁止无信息装饰动画；尊重 reduced-motion。
15. Agent 颜色只是强调，不能作为唯一状态指示。
16. 每个主要页面实现前用设计评审门槛自检（识别度/状态可感知/双主题/记忆点/无障碍）。


## 5. 后端 / Runtime 开发规则

如果任务涉及 Runtime 或本地服务，必须遵守：

1. 命令入参必须校验；带 expected version 防止并发覆盖。
2. 权限、路径、工具、人类专属动作等安全边界必须在 Runtime 侧显式处理。
3. 数据库写入必须考虑事务、一致性和失败回滚；迁移前备份。
4. 错误返回必须稳定、可诊断，不暴露敏感信息。
5. 日志必须有助于排查问题，但不能记录密钥、token、密码。
6. 新增协议消息必须保持与共享类型一致。
7. 涉及分页、排序、搜索时，要明确默认值和边界条件。
8. UI 生命周期不得绑定 Run 生命周期。
9. Worker 最小权限；崩溃只影响其 Step。
10. 不静默替换模型；遵循模型解析优先级。

## 6. 测试与验证规则

### 6.1 测试先行（TDD，项目有测试框架时默认启用）

如果项目已配置测试框架（jest/vitest/pytest/go test/junit 等），实现新功能或修复 bug 时必须遵循：

1. 先写失败测试（Red）：根据需求或 bug 编写最少验证用例，运行确认失败。
2. 最小实现（Green）：写刚好让测试通过的代码，不提前优化。
3. 重构（Refactor）：在测试保护下优化命名、抽象、性能，确保测试仍然通过。
4. 确认所有相关测试通过后，再进入下一步。

Runtime、状态机、权限、适配器、恢复相关能力**优先** TDD。

如果项目没有测试框架，跳过本步骤但在最终回复中标注"本项目未配置测试框架，建议后续补充"。

如果是纯 UI 调整、文档变更或配置修改等不适合单测的场景，跳过 TDD 但必须在验证门禁中提供替代证据。

### 6.2 验证门禁

实现完成后，AI 必须提供以下证据（至少满足 2 项）才能标记任务完成：

- [ ] 测试通过输出（单测、集成、E2E 任一）
- [ ] 构建成功输出（build/compile 无 error）
- [ ] 类型检查通过（tsc/mypy/go vet 等）
- [ ] 接口/协议响应示例
- [ ] 桌面端手动测试步骤（见 §9.1）

无法提供证据时，必须说明原因和剩余风险，不能默认标记完成。

### 6.3 基本验证流程

完成修改后，AI 必须尽可能执行验证：

1. 优先运行与改动相关的最小测试。
2. 如果有 lint/typecheck/build/test 命令，应根据改动风险选择运行。
3. 前端视觉改动需要启动页面并检查实际效果。
4. 修 bug 时必须说明复现原因和验证方式。
5. 如果无法运行测试，必须明确说明原因和剩余风险。

推荐验证顺序：

```text
相关单测 -> 类型检查 -> lint -> build -> 手动功能验证
```

## 7. Git 与文件安全规则

AI 必须遵守：

1. 不执行 `git reset --hard`。
2. 不执行会丢失用户改动的 checkout/revert 操作，除非用户明确要求。
3. 不删除不理解的文件。
4. 不改动锁文件，除非依赖确实发生变化。
5. 不提交代码，除非用户明确要求。
6. 遇到工作区已有改动时，默认认为是用户改动，必须保留。

## 8. 依赖与配置规则

1. 新增依赖前必须说明理由。
2. 能用项目已有依赖解决的问题，不新增依赖。
3. 不随意升级框架、构建工具、语言版本。
4. 配置改动必须说明影响范围。
5. 环境变量必须提供示例，不提交真实密钥。

## 8.0 技术选型确认规则

当任务涉及以下内容时，AI 不能直接决定技术方案，必须先给出方案推荐并等待用户确认：

1. 前端框架、后端框架、构建工具、运行时。
2. 数据库、缓存、消息队列、搜索引擎、对象存储。
3. 鉴权、权限、支付、邮件、短信、推送、第三方登录。
4. 部署平台、CI/CD、容器化、云服务、监控、日志、任务调度。
5. 图表、富文本、地图、表格、文件预览等重型前端依赖。
6. 会影响架构、性能、成本、安全、维护方式的新增依赖或服务。
7. 设计文档第 24 节尚未锁定的具体库（SQLite 驱动、安全存储、UIA 库、组件原语、更新器等）。

技术选型流程必须是：

1. 说明当前技术需求和约束。
2. 给出 2-3 个可选方案。
3. 对比每个方案的目的、优点、缺点、性能影响、成本和维护复杂度。
4. 给出推荐方案和推荐理由。
5. 等待用户确认。
6. 确认后写入 `docs/engineering/04-tech-decisions.md`。
7. 再进入实现。

除非用户明确指定某个技术，并明确表示不需要比较方案，否则 AI 不能跳过本规则。

已锁定的栈方向（可规划，不可擅自推翻）：

- Electron + React/TypeScript
- 独立 Node.js/TypeScript Runtime
- SQLite 真源
- XState 表达 + 事件/检查点
- Playwright Worker
- Windows UI Automation Worker（具体库待 spike）

## 8.1 性能设计规则

AI 在设计方案和实现新功能时必须提前考虑性能：

1. 前端优先考虑按需引入、懒加载、代码分割和资源体积控制。
2. 大型组件、图表、编辑器、地图、富文本等默认不进入首屏主包，除非项目明确需要。
3. 列表、表格、日志、消息流等大数据场景必须考虑分页、虚拟滚动或增量加载。
4. 请求层必须避免重复请求、瀑布请求和过大的响应体。
5. Runtime 必须考虑查询索引、分页、缓存、批量处理、超时和重试边界。
6. 新增依赖必须说明性能影响和是否支持按需引入。
7. 如果为了简单暂时不做性能优化，必须说明原因和后续补点。

## 9. 文档输出规则

当任务涉及文档或交付说明时，AI 应输出：

1. 修改了哪些文件。
2. 核心实现思路。
3. 运行或验证命令。
4. 已知限制或后续建议。

回答要直接、具体，不要堆砌空话。

## 9.1 页面手动测试步骤输出规则

完成一个需求、功能、变更或 bug 修复后，如果涉及用户可见页面、交互流程、接口数据在页面上的表现，AI 在最终回复中必须给出“页面手动测试步骤”。

测试步骤必须能让用户直接在桌面应用或开发窗口中验证，不只是写命令。

输出应包含：

1. 进入哪个页面或窗口。
2. 使用什么前置数据（Provider、文件夹、任务等）。
3. 点击哪些按钮或执行哪些操作。
4. 输入哪些测试数据。
5. 预期看到什么页面变化、提示、数据结果或错误状态。
6. 至少包含一个正常流程。
7. 如果功能有明显边界，补充 1-2 个异常/边界流程。

如果本次改动不涉及页面，例如纯 Runtime、脚本、配置或文档，AI 必须说明“本次无页面手测步骤”，并给出可替代的验证方式。

## 10. 沟通规则

AI 与用户沟通时必须：

1. 先给结论，再给必要解释。
2. 不确定时明确说明不确定点。
3. 不假装已经验证未验证的内容。
4. 不把猜测写成事实。
5. 遇到多种方案时，给出推荐方案和取舍理由。
6. 用户要求直接实现时，不只停留在方案层。
7. 不要让用户重新解释已批准的产品问题；对照设计文档执行。

## 11. 任务完成标准

一个开发任务只有在满足以下条件后才算完成：

1. 需求中的主要功能已经实现。
2. 相关错误和边界情况已处理。
3. 已运行必要验证，或明确说明无法验证的原因。
4. 没有引入明显无关改动。
5. 最终回复包含清晰的完成说明。
6. 行为变化已更新相关 docs / changelog。

## 12. AI 必须主动避免的问题

以下行为默认禁止：

1. 大段重写用户没有要求修改的模块。
2. 用 mock 数据掩盖真实接口问题后声称生产就绪。
3. 删除测试来让结果通过。
4. 用 `any`、空 catch、硬编码超时等方式绕过类型或错误。
5. 将复杂业务逻辑写进 UI 组件而不拆分。
6. 把权限校验只放在前端。
7. 只看文件名猜业务含义。
8. 生成无法运行的伪代码后声称已完成。
9. 静默换模型或实现“自动找最便宜模型”。
10. 在 DB/日志/提示词中留下明文密钥。
11. 让 UI 退出导致 Run 中断。
12. 并行产物 last-write-wins。

## 13. 项目定制区（SYNC-THINK）

### 技术栈

```text
前端：Electron + React + TypeScript
Runtime：独立 Node.js + TypeScript 进程
数据库：SQLite（真源）+ FTS；驱动/迁移框架待 Phase 0 spike
状态：XState 表达 + 持久事件/检查点
UI↔Runtime：Electron main bridge + 认证本地命名管道
浏览器自动化：Playwright Worker
Windows 桌面控制：UI Automation Worker（库待 spike）
MCP：官方 TypeScript SDK + Runtime 侧授权
Provider：OpenAI-compatible（Responses/Chat Completions/Images）+ Anthropic-compatible Messages
凭证：OS-backed secure store（实现待 spike）
部署：Windows 安装包 / 签名 / 更新（Phase 3）
测试：Vitest/Jest 等（Phase 0 选型）；合同夹具；E2E 后续
```

### 代码风格

```text
命名规则：TypeScript 清晰命名；领域实体与设计文档一致（Task/Run/Step/ArtifactVersion 等）
目录规则：apps/* + packages/* 模块化单体；见 development principles
组件规则：token 驱动；克制卡片；聊天优先
接口规则：本地协议类型共享；命令带版本期望
错误处理规则：稳定错误码 + 可行动消息 + 脱敏诊断
```

### 业务红线

```text
权限规则：Runtime 强制；人类专属动作不可绕过
数据隔离规则：跨任务仅显式引用；密钥不进 UI/日志
模型路由规则：用户控制；无静默替换
审计/日志规则：关键动作全审计且脱敏
合规要求：不提取非官方 token/cookie；不共享创建者凭证
```

### 验收标准

```text
功能验收：见 docs/product/01-requirements-clarification.md §8 与设计文档 §23.2
性能验收：长对话可滚动；轨迹可折叠；重型面板按需
兼容性验收：Windows 高 DPI / 缩放；完整浅/深主题
安全验收：无明文密钥；无未授权工具；恢复不重复副作用
```

## 14. 推荐首次对话模板

```text
你现在是本项目的开发助手。

请先阅读并遵守：
1. 项目根目录 AI_DEVELOPMENT_RULES.md
2. docs/00_START_HERE.md
3. docs/superpowers/specs/2026-07-11-sync-think-product-design.md

Locked 决策不要重开。第一原则：上下文连续性与用户控制模型调度。

接下来你需要完成的任务是：
【在这里写具体需求】

要求：
1. 先阅读相关代码和项目结构。
2. 不要修改无关文件。
3. 实现后运行必要验证。
4. 最后说明修改内容、验证结果和剩余风险。
```
