<Window x:Class="SyncThinkDesktopFixture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="560"
        Height="390"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        AutomationProperties.AutomationId="FixtureWindow">
  <Grid Margin="28">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               FontSize="22"
               FontWeight="SemiBold"
               Text="SYNC-THINK Desktop handoff fixture" />
    <TextBlock Grid.Row="1"
               Margin="0,8,0,20"
               Foreground="#5E6470"
               Text="The Apply action blocks long enough for the E2E harness to inject real user input." />

    <TextBox x:Name="InputText"
             Grid.Row="2"
             Height="34"
             Padding="8,5"
             Text="initial"
             TextChanged="InputText_OnTextChanged"
             AutomationProperties.AutomationId="InputText"
             AutomationProperties.Name="Input value" />

    <PasswordBox x:Name="PasswordInput"
                 Grid.Row="3"
                 Height="34"
                 Margin="0,12,0,0"
                 Padding="8,5"
                 Password="fixture-secret"
                 AutomationProperties.AutomationId="PasswordInput"
                 AutomationProperties.Name="Password" />

    <Button x:Name="ApplyButton"
            Grid.Row="4"
            Width="150"
            Height="38"
            Margin="0,18,0,0"
            HorizontalAlignment="Left"
            Content="Apply slowly"
            AutomationProperties.AutomationId="ApplyButton"
            AutomationProperties.Name="Apply slowly"
            Click="ApplyButton_OnClick" />

    <Border Grid.Row="5"
            Margin="0,20,0,0"
            Padding="12"
            Background="#F3F5F7"
            CornerRadius="6">
      <StackPanel>
        <TextBlock FontWeight="SemiBold" Text="Result" />
        <TextBlock x:Name="ResultText"
                   Margin="0,6,0,0"
                   Text="ready"
                   AutomationProperties.AutomationId="ResultText"
                   AutomationProperties.Name="ready" />
      </StackPanel>
    </Border>
  </Grid>
</Window>
