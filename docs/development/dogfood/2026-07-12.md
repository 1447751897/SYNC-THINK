# Dogfood · 2026-07-12（有效 1/1）

> 证据来自当天真实桌面运行、持久数据库事件与重启前后核对；不包含任何 API Key。

## 今天主要干了啥
- [x] 启动桌面端，Runtime 已连接并打开真实任务
- [x] 从 CC Switch 预览并导入 Provider、密钥分组与模型，导入后编辑 Provider
- [x] 使用 KMKAPI 与 Unity2.Ai 两个真实端点，目录中有 3 个以上模型
- [x] 同一任务运行 `grok-4.5`、`gpt-5.6-sol`、`gpt-5.5` 与 Claude 模型路径
- [x] 查看 Agent 默认模型与 fallback 链，保存后重启仍恢复
- [x] 打开 Manifest，检查解析来源、credential ref、Agent version 与 fallback 链位
- [x] 让 Unity2.Ai Claude 主模型真实失败，观察暂停；配置 fallback 后自动切至 `gpt-5.5`
- [x] 折叠 / 展开 Trace，中文类别与最终状态可读
- [x] 重启 Runtime 与桌面，恢复任务、消息、Manifest 与唯一最终回复
- [x] 对真实 vault secret、DB/WAL/备份/日志/诊断和证据导出执行明文扫描

## Provider / 模型
- Provider A：KMKAPI（CC Switch 导入；OpenAI compatible）
- Provider B：Unity2.Ai（CC Switch 导入；Anthropic Messages）
- 实际路径：`grok-4.5`、`gpt-5.6-sol`、`gpt-5.5`、Claude Haiku / Sonnet
- 上下文：同一任务继续运行，Context Packet / Manifest 持续生成，无需重新配置目标
- 外部限制：Unity2.Ai 账户池无额度，返回 503；不是 SYNC-THINK 连接或协议故障

## Manifest / 权限
- Manifest：已打开；可见 included sources、解析来源、Agent version、credential ref、fallbackIndex
- Skill / MCP：该任务无额外 allowlist，Manifest 显示 none，符合绑定配置
- 审批：已有 Approval Center 与策略记录；本次模型调用没有需要人工批准的新工具动作

## Fallback / 恢复
- 主路径：Unity2.Ai `claude-3-5-haiku-20241022` 返回 503
- Fallback：`codex / gpt-5.5`，事件 `run.fallback.selected`，`fallbackIndex: 0`
- 最终输出：`Fallback 自动切换成功。`，仅一条完成消息
- 重启后：任务、32 条消息、fallback trace、Agent v12 默认 `gpt-5.6-sol` + fallback `gpt-5.5` 均恢复
- 重启计数：Events 423、Packet 20、Completed Runs 9，前后不变

## 安全
- 11 个真实 vault secret 对 255 个相关文件逐字节扫描：明文命中 0
- 19058-byte 证据导出：exact secret 0、secret-like token 0

## 阻塞
- 无产品阻塞；Unity2.Ai 当前额度不足，暂时不能用其 Claude 模型取得成功正文

## 摩擦
- 供应商 / 分组 / 模型的归属最初不清晰；当天已改为 Agent 内按分组 → 供应商 → 模型选择
- 外部网关错误原文信息偏多；当天已改为紧凑中文状态与可展开详情

## 一句话结论
- 真实 Provider、Fallback、Manifest、恢复与安全路径可用于继续开发；本日计为有效 dogfood **1/1**。用户于 2026-07-15 确认一天即可，M1 dogfood 门槛已满足。
