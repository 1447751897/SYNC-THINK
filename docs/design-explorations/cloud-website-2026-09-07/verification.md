# 官网视觉与交互验收：2026-09-07

本轮方向来自用户对 Multica 绘画风格的要求，以及后续提供的 Nexeus 提示词。后者作为视觉参考使用；产品仍为 SYNC-THINK，多页账号与 iframe 演示继续保留。

## 对照记录

活跃参考为 `05-hero-painted-concept.png`、`07-faq-footer-painted-concept.png`、`03-login-concept.png`。已使用 `view_image` 将三张参考与最终浏览器截图直接对照，按本轮修订后的视觉方向完成核验，差异如下。

| 检查点 | 参考与最终实现 | 处理结果 |
| --- | --- | --- |
| 图片风格 | 蓝天、粉紫雪山、明显笔触 | 原写实背景替换为原创绘画位图；WebP 1536×1024、350,492 字节 |
| 标题与排版 | 左对齐白色品牌标题、两项主操作 | 使用用户追加参考中的 Instrument Serif；以真实品牌标志替换生成概念中的占位标志 |
| 首屏文案 | 品牌、主标语、说明、下载和登录 | 文案核对通过；顺序保持一致，概念中自动生成的产品区文案以实际产品文案为准 |
| 容器与间距 | 全幅首屏、下方露出产品内容 | 1536×1024 首屏露出 iframe；手机 390×844 标题均衡换行，主体无横向溢出 |
| 产品体验 | 概念用静态示意工作台 | 实现为可交互的原生 iframe；支持提交、审批、三步执行、工具详情与产物，不调用真实模型 |
| 页脚与配色 | 绘画背景、玻璃表面、细分隔线 | 页脚用同一幅原创图与模糊玻璃表面；浅色为白底，深色跟随系统；保留已存在的下载收尾区 |
| 登录表单 | 窄表单、绿色提交按钮、清晰标签 | 使用真实邮箱/密码流程，密码显示采用复选框；未添加尚未确定内容的条款链接 |
| 手机导航 | 顶部紧凑展开菜单 | 修正白底白字，实测菜单可读、选择后关闭，FAQ 最终距视口顶部约 32px |
| 动效 | 内容依次进入、减少动态效果偏好 | 原生 Web Animations API；静态 WebP 配入场动效，不加载参考中的第三方背景视频 |

## 浏览器证据

- 使用内嵌浏览器检查 1536×1024、390×844 的首屏、登录与完整 iframe 流程；截图按 `desktop-hero.png`、`mobile-hero.png`、`mobile-login.png` 和 `mobile-demo.png` 留存在本机，未随仓库发布。
- 独立 Edge/Playwright 复核浅色首页、页脚和登录，尺寸与概念同为 1536×1024；本机截图文件为 `hero-1536-light.png`、`footer-1536-light.png`、`login-1536-light.png`。手机剪贴板降级截图为 `clipboard-fallback-mobile.png`。这些临时证据不作为跨环境文件链接。
- 已修复实际 iframe 沙箱发送失效、手机导航对比度与标题单字落行。复制成功显示目标站点权限要求；剪贴板被拒绝时展示并选中只读代码框，手机无溢出。
- 官网 12 项、云端服务 15 项测试通过；包含实际 iframe 回归。隔离浏览器环境验证注册、验证邮件、登录、退出、密码重置与刷新后继续重置，页面错误为 0。
- 未发现剩余阻断视觉或交互问题。真实 SMTP 投递、生产 HTTPS 与服务器部署尚待正式环境验证；桌面绑定、同步及云端执行不属于本次上线范围。
